language: node_js
node_js:
  - node
  - lts/*
cache:
  directories:
    - ~/.npm
    - .jest
before_script:
  - npm install -g npm@latest
  - npm install
  - npm install jest-expo
  - npm install -g expo-cli
script:
  - npm ci
  - npx jest --ci
install:
  - yarn
jobs:
  include:
    - stage: deploy
      node_js: lts/*
      script:
        - npm ci
