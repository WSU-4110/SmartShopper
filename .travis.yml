language: node_js
node_js:
  - node
  - lts/*
cache:
  directories:
    - ~/.npm
    - .jest
before_script:
script:
install:
  - yarn
jobs:
  include:
    - stage: deploy
      node_js: lts/*
      script:
        - npm ci
