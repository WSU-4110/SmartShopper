language: node_js
node_js:
  - "8.11.3"
  - lts/*
cache:
  directories:
    - ~/.npm
    - .jest
install:
  - yarn
  - npm install -g npm@latest
script:
  echo "npm test temporarily disabled"
  - npm run test

jobs:
  include:
    - stage: deploy
      node_js: lts/*
      script:
        - npm ci
        
        
      
