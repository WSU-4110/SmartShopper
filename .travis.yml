language: node_js
node_js:
  - node
  - lts/*
cache:
  directories:
    - ~/.npm
    - .jest
install:
  - yarn
  
script:
  - npm test --coverage
jobs:
  include:
    - stage: deploy
      node_js: lts/*
      script:
        - npm ci
